<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    tools:context=".activities.QuizActivity"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >


    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <github.chenupt.springindicator.viewpager.ScrollerViewPager
            android:id="@+id/view_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center" />

        <github.chenupt.springindicator.SpringIndicator
            app:siTextSize="18sp"
            android:layout_gravity="bottom"
            app:siIndicatorColors="@array/indicator_colors"
            app:siTextColor="@color/grey_700"
            app:siSelectedTextColor="@android:color/white"
            app:siIndicatorColor="@color/colorPrimary"
            android:id="@+id/indicator"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginLeft="24dp"
            android:layout_marginRight="24dp"
            android:layout_marginBottom="12dp"/>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/exit_from_quiz_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|top"
            android:layout_margin="24dp"
            android:layout_weight="0"
            android:visibility="invisible"
            android:src="@drawable/baseline_exit_to_app_white_48"
            app:backgroundTint="@color/colorPrimary"></android.support.design.widget.FloatingActionButton>

    </FrameLayout>

</RelativeLayout>